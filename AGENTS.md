# SilentGuard Team AGENTS

이 문서는 SilentGuard 프로젝트의 팀 공통 기준 문서다.

모든 AI 에이전트와 팀원은 작업을 시작하기 전에 이 파일을 먼저 읽고, 루트 `data_schema.md`, 자기 담당 영역의 전용 AGENTS와 Phase 문서를 이어서 읽어야 한다.

## 1. 서비스 개요

SilentGuard는 채팅 대화에서 사이버 괴롭힘 위험 신호를 조기에 감지하고, 관련 원본 대화를 관리자가 검토할 수 있는 형태로 정리해주는 서비스다.

이 서비스는 학교폭력, 사이버불링, 가해자, 피해자를 확정 판정하지 않는다. 모델과 Agent는 위험 가능성을 알려주고, 관리자가 원본 대화와 근거를 보고 판단할 수 있도록 돕는다.

초기 MVP의 목표는 실제 운영 서비스가 아니라, 다음 흐름이 끝까지 보이는 로컬 데모를 만드는 것이다.

서버 배포는 하지 않고, 로컬에서 프론트엔드와 백엔드를 각각 실행해 CORS로 연결한다.

```text
front/ 브라우저 화면
-> back/ 백엔드 API
-> ai/ mock 모델 위험 확률 계산
-> back/ 위험 단계 계산
-> warning 이상이면 ai/ Agent가 위험 구간과 사건 정리
-> back/ 프론트엔드 응답 조립
-> front/ 원본 대화, 위험도, Agent 알림, 전달 상태 표시
```

## 2. 해결하려는 문제

채팅 기반 괴롭힘은 한 문장만 보고 판단하기 어렵다. 조롱, 배제, 압박, 협박은 여러 메시지에 나뉘어 나타나고, 관리자가 직접 모든 대화를 읽지 않으면 흐름을 놓치기 쉽다.

SilentGuard는 다음 문제를 줄이기 위해 만든다.

- 위험 신호가 여러 메시지에 흩어져 있어 확인하기 어렵다.
- 반복적인 조롱이나 배제는 맥락을 봐야 이해된다.
- 관리자가 위험 대화를 검토할 때 원본 근거와 시간이 필요하다.
- 위험 상황을 발견했을 때 일관된 알림 형식이 필요하다.

## 3. MVP 범위

첫 MVP에서 반드시 만드는 것:

- 채팅 메시지 입력 또는 샘플 대화 입력
- 모델 또는 mock 모델의 `bullying_probability` 출력
- 백엔드의 `normal`, `caution`, `warning`, `immediate` 위험 단계 계산
- `warning` 이상에서 Agent 분석 실행
- Agent의 위험 구간, 사건 요약, 근거 메시지 ID, 관리자용 알림 생성
- 프론트엔드에서 원본 메시지와 분석 결과 표시
- 실제 외부 전송이 없더라도 알림 전달 상태 표시

첫 MVP에서 하지 않는 것:

- 학교폭력 여부 확정
- 가해자·피해자 확정
- 법적 신고 자동화
- 운영용 사용자 인증과 권한
- 실제 사용자 개인정보 수집
- 운영용 DB와 장기 보관
- MCP를 이용한 판단 또는 분석

## 4. 역할 분리와 디렉터리

`front/`는 채팅 입력, 원본 대화, 위험 확률, 위험 단계, Agent 분석 결과, 알림 전달 상태를 화면에 표시한다. 프론트엔드는 `analysis_id`, `bullying_probability`, `risk_level`, 위험 구간, 사건을 만들지 않는다.

`back/`은 FastAPI API 서버다. 메시지 입력을 검증하고, 모델을 호출하고, 확률을 위험 단계로 바꾼 뒤 Agent와 프론트엔드를 연결한다. 백엔드는 위험 구간이나 사건을 직접 판단하지 않는다.

`ai/model/`은 채팅 입력을 보고 `0`부터 `1` 사이의 괴롭힘 위험 확률만 계산한다. 현재 MVP에서는 mock 모델 결과를 반환한다.

`ai/agent_analysis/`, `ai/notification/`, `ai/schemas/`는 위험 구간 구성, 사건 구성, 위험 유형 분석, 맥락 기반 위험 이유, 관리자용 요약, 초기 조치 참고사항, 알림 내용을 만든다.

`agent/phases/`는 AI 담당자의 전용 Phase 기록이다. 프로젝트 전체 연결 Phase 문서는 루트 `docs/`에 둔다.

`docs/`는 프로젝트 전체 Phase 문서와 `docs/superpowers/` 설계·계획 기록을 둔다.

`test/`는 실제 자동 테스트 코드와 Phase별 실행 결과 Markdown을 둔다.

`legacy/Nvidia_test/`는 과거 NVIDIA 관련 코드, 문서, 테스트를 보존하는 위치다. 현재 실행 구조는 `front/`, `back/`, `ai/`를 기준으로 한다.

## 5. 데이터와 결과 흐름

루트 `data_schema.md`는 프로젝트 전체 데이터 계약의 기준 문서다. 모든 작업자는 입출력, ID 생성 주체, 위험 단계, Agent 결과, 알림 상태를 판단할 때 이 파일을 먼저 따른다.

`data_schema.md`와 코드, Phase 문서, Notion 문서가 충돌하면 임의로 고치지 말고 충돌 지점과 영향 범위를 사용자에게 보고한다.

공통 메시지 입력 형식은 다음을 기준으로 한다.

```json
{
  "message_id": "msg_001",
  "sender_id": "A",
  "sender_label": "A",
  "text": "너 왜 또 여기 들어왔냐",
  "created_at": "2026-08-08T14:28:00+09:00"
}
```

모델 출력은 설명이 아니라 숫자다.

```json
{
  "bullying_probability": 0.91
}
```

백엔드는 이 숫자를 위험 단계로 바꾼다.

```text
0.00 <= probability < 0.50 = normal
0.50 <= probability < 0.70 = caution
0.70 <= probability < 0.85 = warning
0.85 <= probability <= 1.00 = immediate
```

Agent는 `warning` 이상에서 원본 메시지와 모델 결과를 받아 관리자용 분석 결과를 만든다.

## 6. AI 작업 기준

모든 AI 에이전트는 작업 시작 전에 이 문서를 읽는다. 이후 루트 `data_schema.md`, 자신에게 전달된 담당 영역의 전용 지침, overview, 현재 Phase 문서, 관련 Notion 문서를 순서대로 읽는다.

문서 내용이 서로 충돌하면 임의로 판단하지 말고 충돌 지점과 영향 범위를 사용자에게 보고한다. 작업 범위를 벗어난 파일을 수정하지 않으며, 다른 담당 영역과 연결되는 계약은 변경 내용을 문서에 남긴다.

## 7. Notion 기준 문서

Notion 문서는 프로젝트 기준 자료다. 로컬 문서와 Notion 내용이 충돌하면 임의로 고치지 말고, 충돌 내용을 사용자에게 보고한 뒤 최신 결정을 확인한다.

반드시 참고할 Notion 문서:

- 프로젝트 메인: https://app.notion.com/p/3b3590c0a9268069b811d73772da2877
- 진행 순서: https://app.notion.com/p/3b3590c0a9268091b1bdd94e4087f31a
- 모델 담당: https://app.notion.com/p/3b8590c0a926806a876fc6289894117f
- 백엔드 담당: https://app.notion.com/p/3b8590c0a92680b1a8e9ef38fe20bc64
- AI Agent·알림 담당: https://app.notion.com/p/3b8590c0a92680739f32f14ba6863504
- 프론트엔드 담당: https://app.notion.com/p/3b8590c0a92680f2af22cd2737d84a7d
- 사전학습 모델 추천: https://app.notion.com/p/3b3590c0a926803088dbe4007e963d95
- 라벨링: https://app.notion.com/p/3b3590c0a92680f3b013f607f1c4170b

## 8. 작업 시작 규칙

모든 AI 에이전트는 다음 순서로 문서를 읽고 작업한다.

1. 공통 `AGENTS.md`
2. 루트 `data_schema.md`
3. 담당 영역 전용 AGENTS
4. 담당 영역 overview
5. 현재 작업 Phase 문서
6. 관련 Notion 담당 페이지

## 9. 개발 원칙

한 번에 전체 시스템을 만들지 않는다. 현재 Phase 하나를 끝내고, 결과를 해당 Phase 문서에 기록한다.

코드를 작성하기 전에 해당 Phase 문서에서 환경, 폴더 구조, 입력·출력 계약, 테스트 범위가 먼저 합의되어 있어야 한다. 합의되지 않은 런타임, 프레임워크, 폴더, 테스트 파일을 임의로 만들지 않는다.

처음에는 mock 위험 점수로 end-to-end 흐름을 먼저 검증한다. 실제 모델, 실제 MCP 전송, 운영용 DB는 필요성이 확인된 Phase에서 붙인다.

실제 사용자의 개인정보가 포함된 데이터는 사용하지 않는다. 데모 데이터와 공개 데이터셋을 사용할 때도 출처, 사용 조건, 라벨 기준을 기록한다.

모델, 백엔드, Agent, 프론트엔드의 역할을 섞지 않는다. 특히 백엔드가 위험 구간이나 사건을 의미적으로 판단하지 않는다.

Agent는 확정 판정 표현을 쓰지 않는다. "위험 신호", "괴롭힘 가능성", "추가 확인 필요" 같은 표현을 사용한다.

### 함수 작성 규칙

- 코드는 기능별로 작은 함수로 나눈다.
- 작은 함수들을 하나의 상위 함수 또는 클래스 메서드가 순서대로 조합한다.
- 각 함수·메서드 선언 바로 위에는 역할을 설명하는 한국어 한 줄 주석을 붙인다.
- 한 함수에 입력 검증, API 호출, 결과 변환, 출력까지 모두 섞지 않는다.
- 함수 이름만 봐도 입력과 반환 역할을 알 수 있게 작성한다.

## 10. 작업 기록 규칙

작업을 끝낸 뒤 현재 Phase 문서에 다음을 기록한다.

- 한 일
- 바꾼 파일
- 입력과 출력
- 확인한 결과
- 남은 문제
- 다음 담당자에게 넘길 내용

중요한 설계 변경, 테스트 결과, 다음 담당자에게 넘길 내용은 해당 팀원 Phase 문서에 남긴다.

프로젝트 전체 연결 테스트는 루트 `docs/phase_*.md`에 테스트할 내용을 Phase별로 적고, 실제 테스트 코드와 Phase 결과 Markdown은 `test/`에 남긴다.

### Phase status 기록 규칙

각 Phase에는 별도의 status 파일을 둔다.

```text
agent/phases/phase_01_status.md
agent/phases/phase_02_status.md
...
```

status 파일은 길게 작성하지 않고 아래 세 항목만 유지한다. 특히 `현재 사항`은
그 Phase에서 구현한 함수명과 역할을 중심으로 쉽게 쓴다.

```markdown
## 현재 사항
- `함수명`: 함수가 맡은 역할
- 실행 결과가 있으면 한 줄로 기록

## 문제/막힌 지점
- 문제가 없으면 `없음`

## 결정된 사항
- 결정된 내용이 없으면 `없음`
```

Phase를 시작할 때 status 파일이 없으면 즉시 만든다. Phase가 진행되는 동안 현재 사항, 문제 또는 막힌 지점, 결정된 사항이 바뀔 때마다 갱신한다. Phase를 끝낼 때 최종 상태를 status 파일에 기록한 뒤 Phase 문서의 완료 여부를 갱신한다.

status 파일에는 Phase 문서의 체크리스트나 배경 설명을 반복해서 쓰지 않는다. 사용자가 현재 상태, 막힌 이유, 확정된 결정만 빠르게 확인할 수 있도록 작성한다.

Phase 문서는 status와 맞춰 `해야 할 일` 체크리스트를 중심으로 갱신한다. 완료한 구현 항목은 체크하고, 구현하지 못한 항목은 체크하지 않는다.

## 11. 완료 기준

MVP 완료 기준은 다음과 같다.

- 일반 대화는 낮은 위험 확률이 나온다.
- 괴롭힘 위험 대화는 높은 위험 확률이 나온다.
- 백엔드는 확률을 네 단계 위험도로 바꾼다.
- `warning` 이상에서 Agent 응답이 생성된다.
- Agent의 주요 판단은 원본 메시지 ID와 시간을 근거로 가진다.
- 위험 구간과 사건은 Agent가 구성한다.
- 프론트엔드는 원본 대화, 위험 확률, 위험 단계, Agent 결과, 알림 상태를 보여준다.
- 첫 MVP에서는 실제 외부 알림이 없어도 표준 알림 포맷과 전달 상태가 화면에 표시된다.
